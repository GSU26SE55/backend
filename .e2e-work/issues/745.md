# #745 — [IoT] trigger_ota command ACKs success but does not trigger an OTA check

Priority: priority: P2
State: OPEN
URL: https://github.com/GSU26SE55/backend/issues/745

---

## Tóm tắt
Command `trigger_ota` chỉ log TODO rồi publish ACK ok/scheduled; không gọi OTA. OTA thật chỉ chạy lịch hàng giờ.

## Bằng chứng
`firmware-esp32/src/commands/command_handler.cpp:95-100`, periodic path `src/main.cpp:481-490`.

## Tái hiện
Publish trigger_ota và quan sát ACK thành công; không có request firmware-check/download ngay sau đó.

## Mong đợi
ACK success chỉ sau khi enqueue/khởi động OTA check; trả rejected/error nếu không thể; telemetry command state phải phản ánh kết quả thật.
