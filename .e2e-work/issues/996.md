# #996 — [ApiGateway][Application Protection] Required WAF filtering and repeated-limit IP block lifecycle are absent

Priority: priority: P1
State: OPEN
URL: https://github.com/GSU26SE55/backend/issues/996

---

## Phạm vi
`[ApiGateway][Application Protection]` — WAF filtering và repeated-limit IP block lifecycle.

## Lỗi
`overall.md:11497-11511` yêu cầu application filtering và block lifecycle khi liên tục vượt rate limit. Gateway hiện chỉ fixed-window limiter (`GatewayRateLimitingExtensions.cs:13-36`, `Program.cs`); không có WAF policy, durable block state, reconciliation/unblock/audit.

## Mức độ
**P1** — required edge protection/abuse lifecycle absent; limiter đơn lẻ không đáp ứng policy.

## Acceptance criteria
- [ ] Managed WAF/reverse-proxy rules for size/method/path/header/content-type and common malformed request patterns, with false-positive policy.
- [ ] Repeated-limit tracking dùng trusted client identity, durable TTL block, unblock/allowlist và audit.
- [ ] Multi-replica consistent; proxy header trust configured, no user-controlled spoof partition.
- [ ] Metrics/logs redacted, alerting and safe admin inspect/unblock.
- [ ] Regression tests normal traffic, repeated limit, expiry, proxy chain and fail-open/closed behavior.

## Dedupe
#812/#820 sửa rate-limit partitioning; issue này là missing WAF + repeated-offender block lifecycle.
