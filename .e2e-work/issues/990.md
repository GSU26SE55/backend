# #990 — [BatteryService][IoT Clock Drift Audit] Rejected drift never persists the required device incident counter

Priority: priority: P2
State: OPEN
URL: https://github.com/GSU26SE55/backend/issues/990

---

## Phạm vi
`[BatteryService][IoT Clock Drift Audit]`.

## Lỗi
EC-24 yêu cầu reject + metric + persist `IotDevice.ClockDriftIncidentCount`. Batch ingest hiện record metric/reject ở `BatchIngestSensorReadingsCommandHandler.cs:101-131`, nhưng entity không có counter và không persist incident. Repeated drift không hiện trong device monitoring/history.

## Mức độ
**P2** — validation đúng nhưng durable device health/audit signal bị mất.

## Acceptance criteria
- [ ] Migration/property hoặc append-only incident model; atomic increment khi drift reject.
- [ ] Counter scoped đúng device, concurrency safe, không double-count idempotent replay.
- [ ] Expose in Admin detail/monitoring (#966), metric/audit correlation.
- [ ] Threshold/reset/retention policy và optional alert documented.
- [ ] Tests repeated/concurrent drift, valid payload, missing device, retry.

## Dedupe
#906 discards heartbeat resource fields; #966 missing monitoring reads. Lỗi này là required persistent drift incident state.
